# SQL Files - Clean & Simple

## ✅ Main File:

**`RESET-DATABASE.sql`** - This is the ONLY file you need!

It does everything:
- Deletes old triggers/functions/policies
- Creates fresh tables
- Sets up RLS policies
- Creates trigger with error handling
- Complete fresh start

---

## 📋 Instructions:

1. **Go to Supabase Dashboard**
2. **SQL Editor**
3. **Copy ALL SQL from: `RESET-DATABASE.sql`**
4. **Run it**
5. **Done!**

That's it. One file, one command, everything fixed.

---

## Optional File:

- `DELETE-ALL-USERS.sql` - Only if you want to delete user accounts manually (before reset)

---

## Summary:

**One main file: `RESET-DATABASE.sql`**

Clean, simple, effective!

