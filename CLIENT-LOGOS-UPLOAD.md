# Client Logos Upload Instructions

## 📁 Folder Location

Upload all client logos to: `public/clients/`

## 📋 Required Logo Files

Please upload the following logo files with these exact names:

1. **Unemplyd** → `unemployed.png` (or `.jpg`, `.svg`)
2. **Nucleus** → `nucleus.png` (or `.jpg`, `.svg`)
3. **Atoms** → `atoms.png` (or `.jpg`, `.svg`)
4. **Meiporul** → `meiporul.png` (or `.jpg`, `.svg`)
5. **Sri Sai Colors** → `sri-sai-colors.png` (or `.jpg`, `.svg`)

## 🎨 Logo Requirements

- **Format**: PNG, JPG, or SVG (PNG recommended for transparency)
- **Size**: Minimum 200x200px (will be displayed as 96x96px round)
- **Shape**: Square or round logos work best
- **Background**: Transparent or solid color (will be displayed in a round container)

## 📂 File Structure

```
public/
  └── clients/
      ├── unemployed.png
      ├── nucleus.png
      ├── atoms.png
      ├── meiporul.png
      └── sri-sai-colors.png
```

## ✅ After Upload

1. The logos will automatically appear in the "Our Clients" section
2. They will scroll horizontally in a news feed style
3. Each logo will be displayed as a round image with the company name below

## 🔄 Fallback

If a logo file is missing, the component will show the first letter of the company name as a fallback.

---

**Note**: Make sure file names match exactly (case-sensitive on some systems).

